export default function DoubleYouApp() {
  return (
    <div style={{padding:'1rem',border:'1px dashed #ccc',borderRadius:8}}>
      DoubleYouApp component placeholder (replace with real app later)
    </div>
  );
}
